[build-system]
requires = [
    "setuptools",
    "wheel",
    "torch",
]
build-backend = "setuptools.build_meta"

[project]
name = "fbgemm"
version = "0.1.0"
description = "FBGEMM XPU operators for Intel GPUs (temporal)"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "BSD-3-Clause"}
dependencies = []

[project.urls]
Repository = "TBD"
